---
layout: pages
title: Activités
nav:
  order: 2
  icon: duration
---

{%- css "critical" %}{% renderFile "src/assets/sass/components/filters.scss" %}{% endcss %}

<div class="filtres">
  <ul>
    <li class="actif"><span>toutes</span></li>
    {% for typeFamiliesItem in collections.type_families %}
      <li><a href="/activites/{{ typeFamiliesItem | slugify }}/">{% inline_icon typeFamiliesItem %} {{ typeFamiliesItem | safe }}</a></li>
    {% endfor %}
  </ul>
</div>

{%- css "critical" %}{% renderFile "src/assets/sass/components/card.scss" %}{% endcss %}
{% from "macros/card.njk" import card %}

<div class="activites cards">
  {% for entry in collections.activites %}
    {{ card(entry.data.type, entry.data.title, entry.url, entry.data.date, entry.data.distance, entry.data.duration, "list", entry.data.map, loop.index) }}
  {% endfor %}
</div>
