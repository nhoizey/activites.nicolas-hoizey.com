---
layout: pages
title: "Activités"
pagination:
  data: collections.type_families
  size: 1
  alias: type_families_item
  addAllPagesToCollections: false
permalink: "/activites/{{ type_families_item | slugify }}/"
---

{%- css "critical" %}{% renderFile "src/assets/sass/components/filters.scss" %}{% endcss %}

<div class="filtres">
  <ul>
    <li><a href="/activites/">toutes</a></li>
    {% for typeFamiliesItem in collections.type_families %}
      {% if typeFamiliesItem == type_families_item %}
        <li class="actif"><span>{% inline_icon typeFamiliesItem %} {{ typeFamiliesItem | safe }}</span></li>
      {% else %}
        <li><a href="/activites/{{ typeFamiliesItem | slugify }}/">{% inline_icon typeFamiliesItem %} {{ typeFamiliesItem | safe }}</a></li>
      {% endif %}
    {% endfor %}
  </ul>
</div>

{%- css "critical" %}{% renderFile "src/assets/sass/components/card.scss" %}{% endcss %}
{% from "macros/card.njk" import card %}

<div class="activites cards">
  {% for entry in collections.activites | type_family(type_families_item) %}
    {{ card(type=entry.data.type, title=entry.data.title, url=entry.url, date=entry.data.date, distance=entry.data.distance, duration=entry.data.duration, score=entry.data.score, where="list", map=entry.data.map, index=loop.index, env=build.env) }}
  {% endfor %}
</div>
