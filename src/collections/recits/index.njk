---
layout: pages
title: Récits
nav:
  order: 4
  icon: recits
---

{% from "macros/card.njk" import card %}
{%- css "critical" %}{% renderFile "src/assets/sass/components/card.scss" %}{% endcss %}

<div class="recits cards">
  {% for entry in collections.recits %}
    {{ card(type="recits", title=entry.data.title, url=entry.url, date=entry.data.date, distance=entry.data.distance, elevation=entry.data.elevation, image=entry.data.image, where="list", env=build.env) }}
  {% endfor %}
</div>
