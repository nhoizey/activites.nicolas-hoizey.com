<!DOCTYPE html>
<html lang="{{ lang }}">
  <head>
    {% include "head.njk" %}
  </head>

  <body>
    <header role="banner">
      {% if page.url == '/' %}
        <h1 class="sitelogo">{{ pkg.title }}</h1>
      {% else %}
        <p class="sitelogo">
          <a href="/">{{ pkg.title }}</a>
        </p>
      {% endif %}
      {% include "components/navigation.njk" %}
    </header>
    <main role="main">
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </main>
    {% include "components/footer.njk" %}
  </body>

</html>
