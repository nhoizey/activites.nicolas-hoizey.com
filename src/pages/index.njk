---
nav:
  order: 1
  title: Accueil
  icon: home
---

{% from "macros/meta.njk" import meta %}

<p>Bienvenue sur ce site, qui présente les activités sportives de Nicolas Hoizey.</p>

<h2>Dernières activités</h2>

<ul class="activites">
  {% for entry in collections.activites | limit(5) %}
    <li class="card">
      <a href="{{ entry.url }}">
        <h3>{{ entry.data.title }}</h3>
        {{ meta(entry.data.date, entry.data.type, entry.data.distance, entry.data.duration) }}
      </a>
    </li>
  {% endfor %}
</ul>
<p>Découvrez toutes les <a href="/activites/">activités</a>.</p>

<h2>Dans le blog</h2>

<ul class="blog">
  {% for entry in collections.blog %}
    <li class="card">
      <a href="{{ entry.url }}">
        <h3>{{ entry.data.title }}</h3>
        {{ meta(entry.data.date) }}
      </a>
    </li>
  {% endfor %}
</ul>
